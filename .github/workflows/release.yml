name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write
  packages: write
  pull-requests: write

jobs:
  release:
    uses: NorseArchitecture/.github/.github/workflows/release-npm.yml@master
    secrets: inherit
